export interface favoriteTable {
  active: boolean;
  title: string;
  count: string;
  detail: string;
  time: string;
}

export interface Song {
  title: string;
  artist: string;
  link: string;
  thumb: string;
}
